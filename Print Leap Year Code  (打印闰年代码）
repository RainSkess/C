#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int a = 0;
	for (a = 1000; a <= 2000; a++)
	{
		if (a % 4 == 0, a % 100 != 0)
		{
			printf（“闰年（leap year)：%d\n”， a）;
		}
		if (a % 400 == 0)
		{
			printf("闰年(leap year):%d\n", a);
		}
	}
	return 0;
}
