#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>

int main()
{
	int a = 0;
	int b = 0;
	int c = 0;
	printf("输入两个数(please enter two numbers)：");
	scanf("%d %d", &a, &b);
	for (c = 1; c <= a; c++)
	{
		if (a % c == 0 && b % c == 0)
		{
			printf("公约数(common divisor)：%d\n", c);
		}
	}
	return 0;
}
