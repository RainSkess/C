#include<stdio.h>

void my_strcpy(char* arr1, char* arr2)
{
    while (*arr1++ = *arr2++)
    {
        ;    
    }
}
int main()
{
    char arr1[20] = "***********";
    char arr2[] = "hello";//字符串常量不可以被修改
    my_strcpy(arr1, arr2);
    printf("%s\n", arr1);
    return 0;
}
