#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>

int main()
{
	int a = 0;
	int b = 0;
	for (a = 100; a <= 200; a++)
	{
		for (b = 2; b <= a; b++)
		{
			if (b % a == 0)
				break;
		}
		if (b == a)
			printf("素数（prime numbers):%d\n", b);
	}
	return 0;
}
