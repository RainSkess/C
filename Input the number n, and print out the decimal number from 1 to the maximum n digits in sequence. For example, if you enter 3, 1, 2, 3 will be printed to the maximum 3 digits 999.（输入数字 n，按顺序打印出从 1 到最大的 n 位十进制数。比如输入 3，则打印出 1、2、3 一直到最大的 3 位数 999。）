#include<math.h>

static int ret[99999];

int* printNumbers(int n, int* returnSize ) {
 
    int k = 1;
    k = pow( 10, n);
    int i = 0;
    for (i = 1; i < k; i++)
    {
        ret[i - 1] = i;
    }
    *returnSize = --i;
    return ret;
}
